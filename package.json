{
  "scripts": {
    "build": "postcss src/styles.css -o dist/styles.css",
    "build:css": "postcss index.css -o output.css",
    "start": "npm run build && npx tailwindcss build src/styles.css -o dist/styles.css --watch",
    "dev": "next dev"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.0.15"
  }
}
